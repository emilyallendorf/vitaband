/*
 * I2C0: VitaBand PCB TWIM — **SCL P0.27, SDA P0.26** (same net order as app_tmp117_i2c.overlay).
 * For nRF52840 DK jumpers labeled from older docs (SCL=P0.26, SDA=P0.27), append AFTER this file:
 *   app_ambient_pwm_i2c_dkswap.overlay
 * SHT3x-DIS @ 0x45 (ADDR → VDD; use 0x44 if ADDR → GND).
 * printk: Segger RTT (prj_ambient_pwm.conf) — uart0 disabled like bring-up.
 *
 * west build ... -DCONF_FILE=prj_ambient_pwm.conf -DDTC_OVERLAY_FILE=app_ambient_pwm.overlay
 */

#include <zephyr/dt-bindings/pinctrl/nrf-pinctrl.h>

/ {
	chosen {
		/delete-property/ zephyr,console;
		/delete-property/ zephyr,shell-uart;
		/delete-property/ zephyr,uart-mcumgr;
		/delete-property/ zephyr,bt-mon-uart;
		/delete-property/ zephyr,bt-c2h-uart;
	};

	pwmleds {
		status = "disabled";
	};
};

&uart0 {
	status = "disabled";
};

&adc {
	status = "disabled";
};

&i2c1 {
	status = "disabled";
};

/* nRF52: SPI0 default pinctrl can overlap Arduino I2C pins — keep off for I2C0 bring-up */
&spi0 {
	status = "disabled";
};

&spi1 {
	status = "disabled";
};

&spi3 {
	status = "disabled";
};

&pinctrl {
	i2c0_ambient: i2c0_ambient {
		group1 {
			psels = <NRF_PSEL(TWIM_SCL, 0, 26)>,
				<NRF_PSEL(TWIM_SDA, 0, 27)>;
			bias-pull-up;
		};
	};

	i2c0_ambient_sleep: i2c0_ambient_sleep {
		group1 {
			psels = <NRF_PSEL(TWIM_SCL, 0, 26)>,
				<NRF_PSEL(TWIM_SDA, 0, 27)>;
			low-power-enable;
		};
	};
};

&i2c0 {
	status = "okay";
	/delete-property/ compatible;
	compatible = "nordic,nrf-twim";
	pinctrl-0 = <&i2c0_ambient>;
	pinctrl-1 = <&i2c0_ambient_sleep>;
	pinctrl-names = "default", "sleep";
	clock-frequency = <100000>;

	sht3xdis: sht3xdis@22 {
		compatible = "sensirion,sht3xd";
		reg = <0x22>;
	};
};
